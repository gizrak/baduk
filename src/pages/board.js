import React from 'react';
import Board from '../components/board';

const BoardPage = () => {
  return (
    <React.Fragment>
      <Board />
    </React.Fragment>
  );
};

export default BoardPage;
